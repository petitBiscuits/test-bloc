import 'package:flutter/material.dart';

class AlarmsDetail extends StatelessWidget {
  const AlarmsDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
